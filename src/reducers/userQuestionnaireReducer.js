const initialState = {
  loading: false,
  current: null,
  all: [],
};

const userQuestionnaireReducer = (state = initialState, action) => {
  switch (
    action.type
    // case :
  ) {
  }
};

export default userQuestionnaireReducer;
